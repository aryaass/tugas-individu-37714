import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TugasIndividu_37714',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
